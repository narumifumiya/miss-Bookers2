<h2>Books detail</h2>

<table>
  <tr>
    <td><%= image_tag 'default-image.jpeg' %></td>
    <td><%= @book.title %></td>
    <td><%= @book.body %></td>
    <td>
      <% if @book.user == current_user %>
        <%= link_to "Destroy", book_path(@book), method: :delete %>
      <% end %>
    </td>
  </tr>
</table>

<%= @book.user.name %>
